# Firebase тохируулах заавар

Энэ зааврыг дагаснаар апп дээр **нэвтрэлт** ажиллаж, 13 замын хэсгийн (ПД-1 … ПД-13)
ажилчид өөр өөрийн хаягаар нэвтэрч, хэмжилтээ **автоматаар үүлэн рүү илгээдэг**,
тухайн хэсгийн **админ** нь бүх ажилчныхаа өгөгдлийг **нэг Excel** болгож татдаг болно.

Нийт хугацаа: ойролцоогоор **20–30 минут**. Программын мэдлэг шаардахгүй.

> ✅ **1–2-р алхам аль хэдийн хийгдсэн.** `uulzwar-zai` төслийн тохиргоо
> `index.html`-д тавигдсан. Танд үлдсэн нь **3-аас 7-р алхам**.
>
> ⚠️ Тохиргоо тавигдсан тул апп одоо **нэвтрэлт шаардана**. 3–6-р алхмыг
> дуустал нь хийж, хаягуудаа үүсгэтэл ажилчид нэвтэрч чадахгүйг анхаарна уу.
> Тиймээс эдгээр алхмыг **нэг суултаар** дуусгахыг зөвлөе.

---

## 1. Firebase төсөл үүсгэх ✅ (хийгдсэн)

1. https://console.firebase.google.com руу Google хаягаараа орно.
2. **Create a project** (эсвэл **Add project**) дарна.
3. Нэр: `uulzvar-zai` (ямар ч нэр болно) → **Continue**.
4. Google Analytics-ийг **унтраана** (Disable) → **Create project**.
5. Дуусахыг хүлээгээд **Continue**.

## 2. Вэб апп нэмж, тохиргоог хуулах ✅ (хийгдсэн)

1. Төслийн нүүрэн дээрх **`</>`** (Web) дүрсийг дарна.
2. App nickname: `Уулзвар` → **Register app**.
3. Дэлгэц дээр иймэрхүү код гарч ирнэ:

   ```js
   const firebaseConfig = {
     apiKey: "AIzaSyD...",
     authDomain: "uulzvar-zai.firebaseapp.com",
     projectId: "uulzvar-zai",
     storageBucket: "uulzvar-zai.firebasestorage.app",
     messagingSenderId: "123456789012",
     appId: "1:123456789012:web:abc123"
   };
   ```

4. Эдгээр **6 утгыг хуулж авна**.
5. `index.html` файлыг нээгээд эхэн хэсгээс `FIREBASE_CONFIG`-ийг олж, хоосон
   хашилтуудыг өөрийн утгуудаараа солино:

   ```js
   const FIREBASE_CONFIG = {
     apiKey: "AIzaSyD...",
     authDomain: "uulzvar-zai.firebaseapp.com",
     projectId: "uulzvar-zai",
     storageBucket: "uulzvar-zai.firebasestorage.app",
     messagingSenderId: "123456789012",
     appId: "1:123456789012:web:abc123"
   };
   ```

6. Хадгалаад GitHub руу түлхнэ (эсвэл GitHub дээр шууд засаж **Commit changes**).

> 🔓 Эдгээр утгууд **нууц биш**. Вэб апп бүр дээр ил байдаг. Аюулгүй байдлыг
> 5-р алхмын дүрмүүд хангана.

## 3. Нэвтрэлт (Authentication) асаах

1. Зүүн цэс → **Build → Authentication** → **Get started**.
2. **Sign-in method** таб → **Email/Password** → **Enable** (эхнийхийг нь л) → **Save**.

## 4. Өгөгдлийн сан (Firestore) үүсгэх

1. Зүүн цэс → **Build → Firestore Database** → **Create database**.
2. Байршил: `eur3` эсвэл `asia-southeast1` — аль нэгийг нь сонгоно.
3. **Start in production mode** сонгоно → **Create**.
   (Дараагийн алхамд бид өөрсдийн дүрмийг тавина.)

## 5. Аюулгүй байдлын дүрэм тавих ⚠️ (заавал)

Энэ алхмыг **алгасаж болохгүй**. Үүнгүйгээр өгөгдөл хамгаалалтгүй болно.

1. **Firestore Database → Rules** таб руу орно.
2. Дэлгэц дээрх бүх текстийг **устгаад**, энэ репод байгаа
   [`firestore.rules`](./firestore.rules) файлын **бүх агуулгыг** хуулж тавина.
3. **Publish** дарна.

Эдгээр дүрэм юу хийдэг вэ:

| Хэн | Юу хийж чадах | Юу хийж ЧАДАХГҮЙ |
|---|---|---|
| Нэвтрээгүй хүн | юу ч үгүй | бүх зүйл хаалттай |
| Ажилтан | өөрийн хэмжилтээ илгээх, засах | бусдын өгөгдөл харах, өөр ПД рүү бичих, өөрийн эрхээ өөрчлөх |
| Админ | өөрийн ПД-ийн бүх өгөгдлийг харах, устгах | **өөр ПД-ийн өгөгдөлд хүрэх**, хэрэглэгчийн эрх өөрчлөх |

## 6. Хэрэглэгч үүсгэх (хэсэг бүрт 2 хаяг)

Замын хэсэг бүрт **2 хаяг** үүсгэнэ (эхлээд ПД-6-гоороо туршаад,
ажиллаж байвал үлдсэнийг нь үүсгэх нь хялбар):

| Хаяг | Үүрэг | Хэн хэрэглэх |
|---|---|---|
| `pd6.admin@uulzvar.mn` | админ | зөвхөн ПД-6-гийн ахлах |
| `pd6.ajiltan@uulzvar.mn` | ажилтан | ПД-6-гийн **бүх ажилчид хамтдаа** |

ПД-1-ээс ПД-13 хүртэл нийт **26 хаяг**.

> 💡 **Нэг хаягаар олон хүн зэрэг нэвтэрч болно** — Firebase-д зэрэг нэвтрэх
> хязгаар байхгүй. Ажилтан бүр апп дээр **өөрийн нэрээ бичнэ** (утсанд нь
> хадгалагдаж, дараа нь автоматаар орж ирнэ), тиймээс Excel дээр хэн юу
> хэмжсэн нь нэрээр ялгарна.

Хүн бүрт **2 зүйл** үүсгэнэ: (а) нэвтрэх хаяг, (б) эрхийн бүртгэл.

### (а) Нэвтрэх хаяг үүсгэх

1. **Authentication → Users** таб → **Add user**.
2. Email: `pd6.admin@uulzvar.mn`
   *(Жинхэнэ шуудангийн хаяг байх шаардлагагүй — зүгээр л нэвтрэх нэр.
   Гэхдээ нууц үг сэргээх функц ажиллуулах бол жинхэнэ хаяг байх хэрэгтэй.)*
3. Password: 6-аас дээш тэмдэгт → **Add user**.
4. Мөн адилаар `pd6.ajiltan@uulzvar.mn` үүсгэнэ. Ажилтны нууц үгийг тухайн
   хэсгийн бүх ажилчдад тарааж өгнө.

### (б) Эрхийн бүртгэл үүсгэх

1. **Firestore Database → Data** таб.
2. Эхний удаа: **Start collection** → Collection ID: **`users`** → **Next**.

   > ⚠️ Энд заавал **`users`** гэж бичнэ — цахим хаяг биш! Зөв болсныг
   > дараагийн дэлгэц дээрх **«Document parent path»** нь **`/users`** гэж
   > байгаагаар шалгана. Хэрэв тэнд хаяг бичээстэй байвал Cancel дараад
   > дахин эхэлнэ.

3. **Document ID** талбарт **цахим хаягийг ЖИЖИГ үсгээр** бичнэ:
   `pd6.admin@uulzvar.mn`
4. Дараах 3 талбарыг нэмнэ:

   | Field (талбарын **нэр**) | Type | **Утга** (доорх нүдэнд) |
   |---|---|---|
   | `pd` | **number** | `6` |
   | `role` | string | `admin` эсвэл `worker` |
   | `name` | string | `ПД-6 админ` |

   > **`number` олдохгүй бол:** `string ▾` дээр дарахад гарах жагсаалтын
   > 2 дахь мөр нь `number`. Утасны дэлгэц жижиг тул жагсаалтын дотор доош
   > гүйлгэх хэрэгтэй байдаг. Олдохгүй бол Chrome-ийн **⋮ → Desktop site**
   > асаана.
   >
   > Санаа зовох хэрэггүй — санамсаргүйгээр `string` болгосон ч **дүрэм
   > хоёуланг нь ойлгодог** тул апп ажиллана. Гэхдээ `number` нь илүү зөв.

   Дэлгэц дээр яг ингэж харагдана:

   ```
   Document ID  [ pd6.admin@uulzvar.mn ]

     Field [ pd ]        Type [ number ▾ ]
       Number  [ 6 ]

     Field [ role ]      Type [ string ▾ ]
       String  [ admin ]

     Field [ name ]      Type [ string ▾ ]
       String  [ ПД-6 админ ]
   ```

5. **Save**. Дараа нь `pd6.ajiltan@uulzvar.mn`-д мөн адил (`role: worker`).

> 📱 **Утаснаас хийж байгаа бол:** гар автоматаар эхний үсгийг **том**
> болгодог. `users`, `pd`, `role`, `name`, `admin` бүгд **жижиг үсгээр**
> байх ёстой (`Users` ≠ `users`). Бичсэний дараа Shift (⇧) дараад жижиг
> болгоно, эсвэл эхлээд `xpd` гээд `x`-г устгана.
>
> **26 бүртгэл үүсгэх тул компьютерээс хийвэл хамаагүй хурдан.**

> ⚠️ **Хамгийн түгээмэл 3 алдаа:**
>
> 1. **Field-д утгыг бичих.** `Field` бол талбарын *нэр* (`pd`, `role`,
>    `name`), доорх нүд нь *утга* (`6`, `admin`, `ПД-6 админ`).
>    «Овог нэр: Батаа» гэвэл `Овог нэр` нь Field, `Батаа` нь утга.
> 2. **`pd`-г string болгох.** `number` сонговол илүү зөв, гэхдээ дүрэм
>    string-ийг ч тоо болгож ойлгодог тул ажиллана.
> 3. **Цуглуулгын нэрийг `users` биш өөр юм болгох** (`Users` ч болохгүй —
>    том/жижиг үсэг ялгаатай).
>
> ⚠️ Document ID нь Authentication дээр үүсгэсэн хаягтай **яг адилхан**
> (үсэг үсгээрээ), **жижиг үсгээр** байх ёстой. Хэрэв домэйнээ `uulzwar.mn`
> гэж сонгосон бол хоёр газарт адилхан `uulzwar.mn` байна.

### ⚠️ Хаягийн нэр заавал энэ хэлбэртэй байх ёстой

Ажилчид аппад **«ПД-6 ажилтан»** гэж богиноор бичиж нэвтэрдэг. Аппыг үүнийг
хаяг болгож хөрвүүлдэг тул хаягууд нь **яг** дараах хэлбэртэй байх ёстой:

```
pd<дугаар>.ajiltan@<домэйн>      pd6.ajiltan@uulzwar.mn
pd<дугаар>.admin@<домэйн>        pd6.admin@uulzwar.mn
```

Апп `uulzwar.mn` болон `uulzvar.mn` хоёуланг нь оролдоно (эхлээд `uulzwar.mn`).
Өөр домэйн ашиглах бол `index.html` доторх `ACCOUNT_DOMAINS` жагсаалтад нэмнэ.

### Жишээ бүтэц

```
users/
  ├── pd6.admin@uulzvar.mn    → { pd: 6,  role: "admin",  name: "ПД-6 админ" }
  ├── pd6.ajiltan@uulzvar.mn  → { pd: 6,  role: "worker", name: "ПД-6 ажилтан" }
  ├── pd7.admin@uulzvar.mn    → { pd: 7,  role: "admin",  name: "ПД-7 админ" }
  ├── pd7.ajiltan@uulzvar.mn  → { pd: 7,  role: "worker", name: "ПД-7 ажилтан" }
  │   … ПД-13 хүртэл …
  └── pd13.ajiltan@uulzvar.mn → { pd: 13, role: "worker", name: "ПД-13 ажилтан" }
```

> 💡 `role`-ийн хувьд `admin`, `Admin`, `Админ`, `админ` — бүгдийг зөв
> таньдаг болгосон. Админ биш бүх утга (`worker`, `ажилтан`, хоосон г.м.)
> ажилтан гэж тооцогдоно.

### Ажилтныг түр хаах

Тухайн бүртгэлд `active` (**boolean**) = `false` талбар нэмнэ. Тэр хаягаар
нэвтэрч, өгөгдөл илгээх боломжгүй болно. Буцаахдаа `true` болгоно.

> 🔑 **Хуваалцсан хаягийн санамж:** хүн ажлаас гарвал тухайн хэсгийн ажилтны
> нууц үгийг сольж, үлдсэн ажилчдад шинэ нууц үгийг тараана
> (Authentication → Users → тухайн мөрийн ⋮ → Reset password).

### Шинэ хэсгийн (ПД) хаяг нэмэх

ПД-6 ажиллаж эхэлсний дараа бусад хэсгийг нэмэхэд **дүрэм (Rules) өөрчлөх
шаардлагагүй** — дүрэм нь ПД дугаараар ерөнхий бичигдсэн. Хэсэг бүрт зөвхөн
дараах **2 алхам** хийнэ.

Жишээ болгон **ПД-3** нэмье. Доорх `3`-ыг өөрийн дугаараар солино.

> ⚠️ **Домэйн:** ПД-6-даа аль домэйныг ашигласнаа хараад **яг тэрийг нь**
> хэрэглэнэ. Апп `uulzwar.mn`-ээр эхэлж оролдоод, олдохгүй бол `uulzvar.mn`
> гэж дахин оролддог. Хоёуланг нь холилдуулбал төөрөгдөл үүснэ.

#### Алхам 1 — Нэвтрэх хаяг (Authentication)

**Build → Authentication → Users → Add user**, хоёр удаа:

| Email | Нууц үг |
|---|---|
| `pd3.admin@uulzwar.mn` | ахлахад өгөх нууц үг |
| `pd3.ajiltan@uulzwar.mn` | тухайн хэсгийн бүх ажилчид хамтдаа хэрэглэнэ |

#### Алхам 2 — Эрхийн бүртгэл (Firestore → `users`)

**Firestore Database → `users` цуглуулга → Add document**, хоёр удаа.

**Document ID** нь дээрх хаягтай **яг адилхан, жижиг үсгээр** байна.

`pd3.admin@uulzwar.mn` баримт:

| Field | Type | Value |
|---|---|---|
| `pd` | number *(string ч болно)* | `3` |
| `role` | string | `admin` |
| `name` | string | `ПД-3 админ` |

`pd3.ajiltan@uulzwar.mn` баримт:

| Field | Type | Value |
|---|---|---|
| `pd` | number *(string ч болно)* | `3` |
| `role` | string | `worker` |
| `name` | string | `ПД-3 ажилтан` |

> 💡 **Type дээр `number` олдохгүй байвал `string` хэвээр нь үлдээгээд `3` гэж
> бичээрэй — ажиллана.** Апп нь `parseInt` хийж, дүрэм нь `int()`-ээр
> хөрвүүлдэг тул хоёулаа адилхан ойлгоно.
>
> `number`-ыг олох гэвэл: `string ▾` дээр дарахад гарах жагсаалтын **2 дахь
> мөр** нь `number` (string · number · boolean · map · array · null ·
> timestamp · geopoint · reference). Утсан дээр жагсаалт багтахгүй байвал
> Chrome → **⋮ → Desktop site** асаана.

#### Шалгах

Аппад **`ПД-3 ажилтан`** гэж бичихэд доор нь `✓ ПД-3 · ажилтан` гэж гарвал
хаяг зөв хөрвөж байна. Нэвтэрсний дараа дэлгэц дээр `ПД-3` гэж түгжээтэй
харагдана.

#### Түгээмэл алдаа

| Шинж тэмдэг | Шалтгаан |
|---|---|
| «хаяг бүртгэлтэй ч эрх нь тохируулагдаагүй» | Алхам 2 хийгээгүй, эсвэл Document ID зөрүүтэй (том үсэг, өөр домэйн) |
| «замын хэсэг (pd: 1–13) буруу» | `pd` талбар байхгүй, тоо болж хөрвөхгүй утгатай, эсвэл 1–13-аас гадуур. *(String `"3"` бол асуудалгүй — апп ч, дүрэм ч тоо болгож ойлгоно.)* |
| Админ нэвтэрсэн ч ажилтан шиг харагдана | `role` нь `admin` (эсвэл `админ`) биш. Өөр ямар ч утга бол ажилтан болно |
| Хаяг олдохгүй | Домэйн зөрүүтэй — ПД-6-тайгаа ижил домэйн эсэхээ шалга |

> 🔒 **Хэсэг хоорондоо тусгаарлагдсан:** ПД-3-ын админ зөвхөн ПД-3-ын
> өгөгдлийг харна. Өөр хэсгийн өгөгдөлд хүрэх боломжгүй — үүнийг апп биш,
> Firestore-ийн дүрэм өөрөө хамгаална.

## 7. Автомат устгалт (30 хоног) ⏳

Апп илгээлт бүр дээр `expireAt` = *илгээсэн цаг + 30 хоног* гэж бичдэг.
Firestore-д «энэ талбарыг хараарай» гэж хэлэхэд л үлдэнэ.

### Хаанаас хийх вэ

Firestore-ийн TTL тохиргоо ихэвчлэн **Google Cloud Console** дээр байдаг
(Firebase Console дээр `TTL` таб харагдахгүй байж болно).

- Firestore Database дээрх табуудыг **баруун тийш гүйлгээд** `TTL` байвал
  шууд тэндээс.
- Байхгүй бол дэлгэцийн дээд талын **«More in Google Cloud ⌄»** →
  **Time-to-live**.
- Эсвэл шууд линкээр (Firebase-тэй ижил Google хаягаар нэвтэрсэн байх):

  ```
  https://console.cloud.google.com/firestore/databases/-default-/ttl?project=uulzwar-zai
  ```

### ⚠️ Энэ алхмыг ХАМГИЙН СҮҮЛД хийнэ

**Collection group** нь унадаг жагсаалт бөгөөд **аль хэдийн үүссэн**
цуглуулгуудыг л харуулдаг. `measurements` цуглуулга нь эхний хэмжилт
илгээгдэх үед сая үүснэ.

Тиймээс: 6-р алхмаа дуусгаад → аппаараа **нэг туршилтын хэмжилт илгээгээд**
→ дараа нь энд эргэж ирнэ. Эс тэгвээс жагсаалт хоосон байна.

### Тохиргоо

1. **CREATE POLICY** дарна.
2. Дөрвөн талбарыг бөглөнө:

   ```
   Collection group *  [ measurements  ▾ ]
   Timestamp field  *  [ updatedAt       ]
   Expiry offset    *  [ 30              ]
   Unit             *  [ days          ▾ ]
   ```

   | Талбар | Утга | Тайлбар |
   |---|---|---|
   | Collection group | `measurements` | жагсаалтаас сонгоно |
   | Timestamp field | `updatedAt` | цагийн талбарын нэр |
   | Expiry offset | `30` | хэдэн нэгжийн дараа устгах |
   | Unit | `days` | нэгж |

3. **CREATE** дарна. Статус эхлээд *Creating*, хэдэн минутын дараа *Active*
   болно.

> **Unit-д `days` байхгүй бол:** Offset = `2592000`, Unit = `seconds`
> (30 хоног = 2,592,000 секунд).
>
> **Өөр хувилбар:** Timestamp field = `expireAt`, Expiry offset = `0`.
> Апп `expireAt`-ыг аль хэдийн «одоо + 30 хоног» гэж бичдэг тул үр дүн ижил.
>
> Хуучин хувилбарын Console дээр зөвхөн **Collection group** ба
> **Timestamp field** гэсэн 2 талбар байдаг. Тэр тохиолдолд
> `measurements` / `expireAt` гэж бөглөнө.

### Юу болох вэ

- Хугацаа дуусмагц Firestore баримтыг өөрөө устгана. Устгалт нь хугацаа
  дууссанаас хойш ихэвчлэн **24 цагийн дотор** явагдана — яг тэр агшинд биш.
- Ажилтан дахин илгээх бүрд хугацаа шинэчлэгдэнэ (сүүлийн илгээснээс 30 хоног).

### Шалгах

Ажилтнаар нэг хэмжилт илгээгээд **Firestore → Data → `measurements`** дотор
баримтыг нээж, `expireAt` талбар байгаа эсэх, огноо нь **өнөөдрөөс 30
хоногийн дараа** гэж байгааг хараарай.

> ⚠️ **Админ Excel-ээ 30 хоногийн дотор татаж авах** хэрэгтэй. Апп дээр
> админы самбарт энэ тухай сануулга гарч байгаа.
>
> Утсан дээрх дотоод санах ойгоос ч мөн 30 хоногоос хуучин хэмжилтүүд
> **энэ тохиргооноос үл хамааран** автоматаар цэвэрлэгдэнэ.

### Хийж чадахгүй бол

Google Cloud Console нээгдэхгүй бол энэ алхмыг **алгасаж болно** — апп хэвийн
ажиллана, зөвхөн үүлэн дэх өгөгдөл өөрөө устахгүй. Админ өөрийн хэсгийн
бичлэгийг гараар устгаж болно (дүрэм зөвшөөрдөг).

## 8. Шалгах

1. Апп-аа утсаараа (эсвэл компьютерийн хөтчөөр) нээнэ.
2. Нэвтрэх цонх гарч ирвэл 2-р алхам зөв хийгдсэн.
3. Ажилтны хаягаар (`pd6.ajiltan@…`) нэвтэрнэ → дээд талд
   **ПД-6 · ажилтан** гэж гарна.
4. **«Хэмжилт хийсэн (нэр)»** талбарт өөрийн нэрээ бичнэ (нэргүй бол эхлэхгүй).
   Хэдэн хэмжилт хийгээд `↓` → **☁️ Илгээх** дарна.
5. Гараад админы хаягаар нэвтэрнэ → **«Хэсгийн өгөгдөл (админ)»** самбарт
   тэр илгээлт харагдана → **📊 Бүгдийг нэг хүснэгтээр Excel татах**.
6. Хуучин, утсанд хадгалагдсан хэмжилтийг илгээхийг шалгах:
   ажилтнаар нэвтрээд **«📂 Өмнөх хэмжилтүүд»** дотор мөр бүрийн **☁️** товч,
   эсвэл дээд талын **«☁️ Илгээгээгүй N хэмжилтийг илгээх»** дарна. Илгээгдсэн
   мөр **☁️ илгээсэн** болж, админд шинээр харагдана.

---

## Хэрхэн ажилладаг вэ

```
Ажилтан (ПД-6)                     Firebase                    Админ (ПД-6)
─────────────                      ────────                    ────────────
нэвтэрнэ (хэсгийн нийтийн хаяг) ─►  Authentication
нэрээ бичнэ
хэмжилт хийнэ
  └─ 20 сек тутам автоматаар ────►  Firestore
«☁️ Илгээх» ───────────────────►  measurements/
📂 хадгалсан хэмжилт → ☁️ ─────►      ▲ (хожим ч илгээж болно)
                                        │
                                        └─ зөвхөн ПД-6 ──────►  нэвтэрнэ
                                                                бүх өгөгдөл татна
                                                                📊 нэг Excel
```

- **Автомат хадгалалт**: хэмжилт хийж байх үед 20 секунд тутам үүлэн рүү
  чимээгүй хадгалагдана. Утас унтарсан ч өгөгдөл алдагдахгүй.
- **Офлайн**: интернэтгүй газар хэмжилт хийж болно. Холбогдмогц автоматаар
  нийлнэ (Firestore офлайн кэш).
- **ПД тусгаарлалт**: ПД-6-гийн админ ПД-7-гийн өгөгдлийг **харах боломжгүй**.
  Үүнийг серверийн түвшинд дүрмээр хаасан — апп-ыг өөрчилсөн ч тойрч гарах
  боломжгүй.
- **Хадгалах хугацаа**: 30 хоног, дараа нь автоматаар устана (7-р алхам).
- **Хэн хэмжсэн**: нэг хаягийг олон хүн хуваалцсан ч апп дээр бичсэн нэрээр
  Excel-д тусад нь гарна.

## Зардал

Firebase-ийн үнэгүй багц (Spark):

| Хязгаар | Үнэгүй / өдөр | Танай хэрэглээ (ойролцоогоор) |
|---|---|---|
| Уншилт | 50,000 | админ 1 удаа татахад ~50 |
| Бичилт | 20,000 | ажилтан 1 өдөрт ~50–200 |
| Хадгалалт | 1 ГБ | жилд ~20 МБ |

13 хэсгийн бүх ажилчид өдөр бүр ажилласан ч **үнэгүй багцад багтана**.
Карт холбох шаардлагагүй.

## Алдаа зассан асуултууд

**«хаяг Firebase-д бүртгэлтэй ч эрх нь тохируулагдаагүй байна»**
→ 6(б) алхам хийгдээгүй, эсвэл Document ID буруу бичигдсэн (том/жижиг үсэг,
үсгийн алдаа). Firestore → `users` дотор хаягийг яг таг шалгана.

**«хаяг Firebase-д бүртгэлтэй ч эрх нь тохируулагдаагүй» гэж байна**
→ Document ID нь Authentication дээрх хаягтай яг таарахгүй байна. Утасны гар
эхний үсгийг том болгосон байж магадгүй (`Pd6…` ≠ `pd6…`). Мөн цуглуулгын
нэр `users` мөн эсэхийг шалгана (`Users` болохгүй).

**Админ нэвтэрсэн ч «Одоогоор ирсэн өгөгдөл алга» гэж байна**
→ Ажилтны `pd` админы `pd`-тэй таарахгүй байна (жишээ нь нэг нь `6`, нөгөө
нь `16`). Хоёуланг нь шалгана.
→ Эсвэл ажилтан хараахан юу ч илгээгээгүй байна.

**«Missing or insufficient permissions»**
→ 5-р алхам (дүрэм) хийгдээгүй эсвэл дутуу хуулагдсан. `firestore.rules`-ийг
бүхэлд нь дахин хуулж **Publish** дарна.

**Нэвтрэх цонх огт гарахгүй байна**
→ Хуучин хувилбар кэшлэгдсэн байна. Аппаа хааж дахин нээнэ үү.

**Excel дээр бүгд ижил нэртэй гарч байна**
→ Ажилчид «Хэмжилт хийсэн (нэр)» талбарт өөр өөрийн нэрээ бичих ёстой.
Нэр нь утсанд нь хадгалагддаг тул нэг л удаа бичихэд хангалттай.

**Өгөгдөл алга болсон**
→ 30 хоногийн хугацаа дууссан байж магадгүй (7-р алхам). Админ Excel-ээ
сар бүр татаж, компьютер дээрээ хадгалж байхыг зөвлөе.

**TTL таб олдохгүй байна**
→ Firebase Console дээр байхгүй байж болно. Google Cloud Console-ийн шууд
линкийг ашиглана (7-р алхам).

**TTL-ийн Collection group жагсаалт хоосон байна**
→ `measurements` цуглуулга хараахан үүсээгүй байна. Аппаараа нэг хэмжилт
илгээсний дараа эргэж ирнэ (7-р алхам).

**«Firebase сан ачаалагдсангүй»**
→ Тухайн утас анх удаа интернэтгүй нээгдсэн. Нэг удаа интернэттэй үед нээвэл
кэшлэгдээд дараа нь офлайн ажиллана.
